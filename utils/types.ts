export type Card = {
    id: number;
    title: string;
    description: string;
    details: string;
};

export type FeatureCard = {
    id: number;
    title: string;
    icon: string;
    color: string;
};